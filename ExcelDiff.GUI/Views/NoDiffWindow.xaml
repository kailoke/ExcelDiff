<Window x:Class="ExcelDiff.GUI.Views.NoDiffWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:props="clr-namespace:ExcelDiff.GUI.Properties"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        SizeToContent="Height"
        Width="420"
        FontFamily="Microsoft YaHei UI, Segoe UI"
        Loaded="NoDiffWindow_Loaded"
        PreviewKeyDown="NoDiffWindow_PreviewKeyDown">
  <Window.Resources>
    <Style x:Key="CloseXButtonStyle" TargetType="Button">
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Width" Value="36"/>
      <Setter Property="Height" Value="36"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bd"
                    CornerRadius="4"
                    Background="Transparent"
                    BorderBrush="#66FFFFFF"
                    BorderThickness="1">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="#2EFFFFFF"/>
                <Setter TargetName="Bd" Property="BorderBrush" Value="White"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="#59FFFFFF"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="DialogButtonStyle" TargetType="Button">
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="FontWeight" Value="Bold"/>
      <Setter Property="Width" Value="120"/>
      <Setter Property="Height" Value="40"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bd"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="8">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Opacity" Value="0.9"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Bd" Property="Opacity" Value="0.74"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <Window.Triggers>
    <EventTrigger RoutedEvent="Window.Loaded">
      <BeginStoryboard>
        <Storyboard>
          <DoubleAnimation Storyboard.TargetName="FlashOverlay"
                           Storyboard.TargetProperty="Opacity"
                           From="0" To="0.55" Duration="0:0:0.28"
                           AutoReverse="True" RepeatBehavior="3x"/>
          <ColorAnimation Storyboard.TargetName="TitleBarBorder"
                          Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                          From="#2E7D32" To="#66BB6A" Duration="0:0:0.28"
                          AutoReverse="True" RepeatBehavior="3x"/>
        </Storyboard>
      </BeginStoryboard>
    </EventTrigger>
  </Window.Triggers>

  <Border Margin="14"
          Background="#FFFFFF"
          BorderBrush="#D0D7DE"
          BorderThickness="1"
          CornerRadius="10">
    <Border.Effect>
      <DropShadowEffect Color="#40000000" BlurRadius="18" ShadowDepth="6" Direction="270" Opacity="0.30"/>
    </Border.Effect>

    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="46"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

      <Border x:Name="TitleBarBorder"
              Grid.Row="0"
              Background="#2E7D32"
              CornerRadius="9,9,0,0"
              MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
        <Grid>
          <TextBlock x:Name="TitleText"
                     Foreground="White"
                     FontSize="15"
                     FontWeight="Bold"
                     VerticalAlignment="Center"
                     Margin="16,0,0,0"/>
          <Rectangle x:Name="FlashOverlay"
                     Fill="White"
                     Opacity="0"
                     IsHitTestVisible="False"/>
          <Button x:Name="CloseButton"
                  Style="{StaticResource CloseXButtonStyle}"
                  Content="&#x2715;"
                  HorizontalAlignment="Right"
                  VerticalAlignment="Center"
                  Margin="0,0,8,0"
                  Click="CloseButton_Click"/>
        </Grid>
      </Border>

      <StackPanel Grid.Row="1" Margin="28,24,28,22">
        <TextBlock x:Name="MessageText"
                   TextWrapping="Wrap"
                   TextAlignment="Center"
                   FontSize="14"
                   Foreground="#37474F"/>

        <StackPanel x:Name="ButtonsRow"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,22,0,0">
          <Button x:Name="CloseResultButton"
                  Style="{StaticResource DialogButtonStyle}"
                  Background="#FFEBEE"
                  Foreground="#C62828"
                  BorderBrush="#FFCDD7E5"
                  BorderThickness="1"
                  Width="200"
                  Margin="0"
                  IsDefault="True"
                  Click="CloseResultButton_Click">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
              <TextBlock Text="{x:Static props:Resources.Word_Exit}"/>
              <TextBlock Text="  Enter" FontSize="11" FontWeight="Normal" Opacity="0.8" VerticalAlignment="Center"/>
            </StackPanel>
          </Button>
          <Button x:Name="OkButton"
                  Style="{StaticResource DialogButtonStyle}"
                  Background="#2E7D32"
                  Foreground="White"
                  BorderThickness="0"
                  Width="120"
                  Margin="12,0,0,0"
                  Click="CloseButton_Click">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
              <TextBlock Text="确定"/>
              <TextBlock Text="  Esc" FontSize="11" FontWeight="Normal" Opacity="0.8" VerticalAlignment="Center"/>
            </StackPanel>
          </Button>
        </StackPanel>
      </StackPanel>
    </Grid>
  </Border>
</Window>
