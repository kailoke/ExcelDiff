<Window x:Class="ExcelDiff.GUI.Views.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExcelDiff.GUI.Views"
        xmlns:app="clr-namespace:ExcelDiff.GUI"
        mc:Ignorable="d"
        Title="{x:Static app:App.DisplayName}" 
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Icon="pack://application:,,,/app.ico"
        Height="120" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label x:Name="Message" Grid.Row="0" Margin="5"
               VerticalAlignment="Center" HorizontalAlignment="Left"/>
        <ProgressBar Grid.Row="1" Margin="10" IsIndeterminate="True" VerticalAlignment="Center" HorizontalAlignment="Stretch" Height="20"/>
    </Grid>
</Window>
